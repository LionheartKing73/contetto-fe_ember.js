import utilUser from 'contetto/utils/util-user';
import { module, test } from 'qunit';

module('Unit | Utility | user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = utilUser();
  assert.ok(result);
});
