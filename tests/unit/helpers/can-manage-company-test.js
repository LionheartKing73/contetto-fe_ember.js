
import { canManageCompany } from 'contetto/helpers/can-manage-company';
import { module, test } from 'qunit';

module('Unit | Helper | can manage company');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canManageCompany([42]);
  assert.ok(result);
});

