
import { isEditRoute } from 'contetto/helpers/is-edit-route';
import { module, test } from 'qunit';

module('Unit | Helper | is edit route');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEditRoute([42]);
  assert.ok(result);
});

