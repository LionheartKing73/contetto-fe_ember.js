
import { canManageBrand } from 'contetto/helpers/can-manage-brand';
import { module, test } from 'qunit';

module('Unit | Helper | can manage brand');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canManageBrand([42]);
  assert.ok(result);
});

