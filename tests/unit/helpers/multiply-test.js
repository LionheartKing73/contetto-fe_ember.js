
import { multiply } from 'contetto/helpers/multiply';
import { module, test } from 'qunit';

module('Unit | Helper | multiply');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = multiply([42]);
  assert.ok(result);
});

